import { Placeholder } from "@/components/placeholder";
export default function P() { return <Placeholder title="Workflows" desc="Visual workflow orchestration — deterministic control flow around agents." />; }
